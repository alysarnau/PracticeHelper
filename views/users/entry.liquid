{% block content %}

<div class="card md-4 shadow">
    <div class="card-body">
        <p>Instrument: {{entry.instrument}}</p>
        <p>Piece: {{entry.piece}}</p>
        <p>Composer: {{entry.composer}}</p>
        <p>Minutes: {{entry.minutes}}</p>
        <p>Notes: {{entry.note}}</p>
        <div class="d-flex justify-content-between">
            <small>author: {{username}}</small>
            <form action='/comments/delete/{{practice._id}}/{{entry._id}}?_method=DELETE' method="POST">
                <input type="submit" value="X">
            </form>
        </div>{% block content %}

        <div class="card md-4 shadow">
            <div class="card-body">
                <p>{{comment.note}}</p>
                <div class="d-flex justify-content-between">
                    <small>author: {{username}}</small>
                    <form action='/comments/delete/{{fruit._id}}/{{comment._id}}?_method=DELETE' method="POST">
                        <input type="submit" value="X">
                    </form>
                </div>
            </div>    
        </div>
        
        {% endblock %}
    </div>    
</div>

{% endblock %}